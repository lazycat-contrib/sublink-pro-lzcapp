params:
  - id: image_tag
    type: string
    name: "Image Tag"
    description: "Docker image tag for sublink-pro (e.g. v1.2.7)"
    default_value: "v1.2.7"
    optional: false

locales:
  zh:
    image_tag:
      name: "镜像版本"
      description: "sublink-pro 的 Docker 镜像版本号（如 v1.2.7）"
  en:
    image_tag:
      name: "Image Tag"
      description: "Docker image tag for sublink-pro (e.g. v1.2.7)"
